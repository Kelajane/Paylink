{
    "configurations": [
        {
            "type": "node-terminal",
            "name": "Run Script: preview",
            "request": "launch",
            "command": "npm run preview",
            "cwd": "${workspaceFolder}"
        }
    ]
}